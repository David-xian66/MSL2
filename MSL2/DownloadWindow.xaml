<Window x:Class="MSL2.DownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MSL2"
        mc:Ignorable="d"
        Title="MSL2-Downloading..." Height="103.238" Width="397.684" Loaded="Window_Loaded" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" Background="{x:Null}" AllowsTransparency="True">
    <Border x:Name="mainBorder" CornerRadius="5" Margin="5,4,5,5">
        <Border.Background>
            <SolidColorBrush Color="White"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <TextBlock x:Name="infolabel" HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="加载中……" VerticalAlignment="Top" FontSize="16"/>
            <ProgressBar x:Name="pbar" HorizontalAlignment="Left" Height="10" Margin="10,37,0,0" VerticalAlignment="Top" Width="369"/>
            <Button Content="取消" HorizontalAlignment="Left" Margin="304,52,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click" FontFamily="Microsoft YaHei UI" FontSize="14" FontWeight="Normal"/>
            <Label x:Name="taskinfo" Content="当前任务：" HorizontalAlignment="Left" Margin="10,52,0,0" VerticalAlignment="Top"/>
        </Grid>
    </Border>
</Window>
